// import settings from config
// import translations from translations

export const localize = (text) => {
    // This function will map the incoming text in English to
    // the desired translation.
    return text;
}